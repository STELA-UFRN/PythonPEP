idm:
    restart: always
    image: fiware/idm
    ports:
        - 8000:8000
        - 5000:5000

pep-proxy:
    restart: always
    image: fiware/pep-proxy
    ports:
        - 80:80
    volumes:
        - ~/Projetos/fiware/pep-proxy/config.js:/opt/fiware-pep-proxy/config.js